(function() {
    var appMenu = angular.module("tgpMenu", [], function($interpolateProvider) {
        $interpolateProvider.startSymbol('[[');
        $interpolateProvider.endSymbol(']]');
    });
})();
